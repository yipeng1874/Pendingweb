# 飞书通知开发者文档

## 1. 文档目标

本文档用于系统性说明当前项目中的"飞书任务通知"能力，重点覆盖：

- 日常任务飞书通知的接口、筛选口径与页面入口
- 日常任务**自动定时通知**的接口、调度逻辑与配置方式
- 临时任务飞书通知的接口、筛选口径与页面入口
- 协同任务流转模式的节点完成提醒（2026-09-03 新增，见第 16 章）
- 临时任务触达式、主播式、管理式完成提醒（2026-09-03 新增，见第 17 章）
- 前后端代码目录、关键函数、关键字段与数据流
- 数据库表与字段如何支撑通知计算
- 后续开发者与 AI 工具接手时的推荐阅读顺序与维护注意事项

本文档的目标不是介绍飞书登录绑定，而是专门说明：

> 任务模块里，系统如何计算"谁该被通知"、如何调用飞书发送、以及前端在哪里触发通知。

---

## 2. 功能概述

当前项目中的飞书通知能力分为两条主线：

1. **日常任务飞书通知**（含手动发送和自动定时发送）
2. **临时任务飞书通知**

两者都基于：
- 用户表中的飞书绑定信息
- 飞书企业配置表中的 `appId / appSecret`
- 任务记录表中的任务完成状态

但两者在人群计算方式上有明显差异：

### 2.1 日常任务通知

日常任务通知更接近"按当天任务记录催办"。

主要特点：
- 面向某个基地当天的日常任务
- 只通知当天仍有**必填子任务未完成**的人
- 已被免除的任务不纳入通知
- 同一人按当天口径只统计一次
- 发送文案统一为：
  - `前缀，日常任务共 x 项，您还有 x 项未完成。`
- 支持**手动发送**和**自动定时发送**两种模式

### 2.2 临时任务通知

临时任务通知更接近"按某条进行中的 assignment 催办"。

主要特点：
- 只允许对**进行中的临时任务 assignment**发通知
- 只通知 `pending / in_progress` 的主体
- 人群计算复用临时任务现有主体分组逻辑
- 支持 `ACCOUNT / ANCHOR / MANAGER` 三种模式
- 业务规则当前按"主体统计不合并"实现预览与统计
- 发送文案当前统一为：
  - `前缀，临时任务共 x 件，您还有 x 件没完成。`

---

## 3. 前后端代码目录

## 3.1 前端相关目录

```text
frontend/src/pages/tasks/dashboard/
├─ DailyTaskDashboardPage.tsx               # 日常任务看板，含"通知今日待办"入口
└─ TemporaryTaskDashboardPage.tsx           # 临时任务看板，含"通知未完成"入口

frontend/src/pages/tasks/manage/
├─ DailyIssuePage.tsx                       # 日常任务发布页入口
└─ TemporaryIssuePage.tsx                   # 临时任务发布页入口

frontend/src/pages/tasks/manage/components/
├─ DailyTaskWizard.tsx                      # 日常任务发布流程组件，含"发送飞书通知"入口
└─ TemporaryIssuePanel.tsx                  # 临时任务发布流程组件，含"通知未完成"入口

frontend/src/services/
└─ task.ts                                  # notifyApi / assignmentApi / reportApi 等服务封装

frontend/src/types.ts                       # 通知预览、发送结果、任务相关共享类型
```

## 3.2 后端相关目录

```text
backend/src/modules/task/notify/
└─ notify.routes.ts                         # 飞书通知接口主入口：日常 + 临时 + 自动调度

backend/src/modules/task/assignment/
├─ daily-assignment.utils.ts                # 日常任务受众计算工具
├─ temporary-assignment.utils.ts            # 临时任务主体分组工具
└─ assignment.service.ts                    # 临时任务草稿/发布/预览服务

backend/src/modules/task/record/
└─ daily-record-time.utils.ts               # 日常任务 record 状态口径

backend/src/app.ts                          # 将 notifyRoutes 挂到 /api

backend/prisma/
└─ schema.prisma                            # TaskAssignment / TaskRecord / User / FeishuEnterpriseConfig / DailyNotifySchedule 等模型
```

## 3.3 建议阅读顺序

建议新维护者按以下顺序阅读：

1. `backend/src/modules/task/notify/notify.routes.ts`
2. `backend/src/modules/task/assignment/temporary-assignment.utils.ts`
3. `backend/src/modules/task/assignment/daily-assignment.utils.ts`
4. `backend/src/modules/task/assignment/assignment.service.ts`
5. `backend/prisma/schema.prisma`
6. `frontend/src/services/task.ts`
7. `frontend/src/pages/tasks/dashboard/DailyTaskDashboardPage.tsx`
8. `frontend/src/pages/tasks/manage/components/DailyTaskWizard.tsx`
9. `frontend/src/pages/tasks/dashboard/TemporaryTaskDashboardPage.tsx`
10. `frontend/src/pages/tasks/manage/components/TemporaryIssuePanel.tsx`

---

## 4. 页面入口与交互位置

## 4.1 日常任务通知入口

### A. 日常任务看板
- 文件：`frontend/src/pages/tasks/dashboard/DailyTaskDashboardPage.tsx`
- 入口按钮：`通知今日待办`
- 当前交互：
  1. 点击按钮
  2. 调用预览接口
  3. 打开页面内确认弹窗
  4. 在弹窗中填写通知前缀
  5. 确认发送

### B. 日常任务发布页
- 文件：`frontend/src/pages/tasks/manage/components/DailyTaskWizard.tsx`
- 入口按钮：`发送飞书通知`
- 当前交互与看板一致：
  1. 点击按钮
  2. 调用预览接口
  3. 打开页面内确认弹窗
  4. 在弹窗中填写通知前缀
  5. 确认发送

## 4.2 临时任务通知入口

### A. 临时任务看板
- 文件：`frontend/src/pages/tasks/dashboard/TemporaryTaskDashboardPage.tsx`
- 入口按钮：`通知未完成`
- 使用场景：对选中的一条进行中的临时任务执行催办

### B. 临时任务发布页
- 文件：`frontend/src/pages/tasks/manage/components/TemporaryIssuePanel.tsx`
- 入口按钮：`通知未完成`
- 使用场景：发布后或进行中任务列表中，对单条进行中的临时任务执行催办

---

## 5. 后端接口说明

## 5.1 日常任务通知接口

### 5.1.1 预览接口
- 方法：`GET`
- 路径：`/api/tasks/notify/daily-feishu/preview`
- 文件：`backend/src/modules/task/notify/notify.routes.ts`

#### Query 参数
- `taskDate?: string`
  - 可选
  - 不传时默认当天北京时间日期
- `scopeOrgId?: string`
  - HQ / DEV 场景下用于选择基地

#### 返回结构
```json
{
  "taskDate": "2026-05-30",
  "scopeOrg": { "id": "...", "name": "河北基地", "orgType": "BASE" },
  "prefixPlaceholder": "来自河北基地提醒",
  "total": 12,
  "pendingCount": 7,
  "inProgressCount": 5,
  "boundCount": 10,
  "unboundCount": 2,
  "configSummaries": [
    { "feishuConfigId": "...", "total": 10, "pending": 6, "inProgress": 4 }
  ],
  "unboundUsers": [
    { "userId": "...", "nickname": "张三", "phone": "138...", "status": "pending" }
  ]
}
```

### 5.1.2 发送接口
- 方法：`POST`
- 路径：`/api/tasks/notify/daily-feishu/send`
- 文件：`backend/src/modules/task/notify/notify.routes.ts`

#### Body 参数
- `taskDate?: string`
- `scopeOrgId?: string`
- `prefix: string`

#### 返回结构
```json
{
  "taskDate": "2026-05-30",
  "prefix": "来自河北基地提醒",
  "scopeOrg": { "id": "...", "name": "河北基地", "orgType": "BASE" },
  "summary": { "...同预览摘要..." },
  "results": [
    {
      "feishuConfigId": "...",
      "configName": "河北基地飞书企业",
      "targetCount": 10,
      "successCount": 9,
      "invalidOpenIds": ["ou_xxx"],
      "messageId": "om_xxx",
      "error": "可选错误信息"
    }
  ]
}
```

## 5.2 日常任务自动定时通知接口

> 这是新增的自动通知能力，支持按基地配置定时自动发送日常催办通知。

### 5.2.1 查询自动通知配置
- 方法：`GET`
- 路径：`/api/tasks/notify/daily-schedule`

#### Query 参数
- `scopeOrgId?: string`

#### 返回结构
```json
{
  "scopeOrg": { "id": "...", "name": "河北基地", "orgType": "BASE" },
  "enabled": false,
  "intervalHours": 3,
  "prefix": "来自河北基地提醒",
  "prefixPlaceholder": "来自河北基地提醒",
  "sharedByBase": true,
  "lastTriggeredSlot": "2026-06-17 09",
  "options": [
    { "intervalHours": 12, "label": "每天2次", "description": "00:00、12:00" },
    { "intervalHours": 6, "label": "每天4次", "description": "00:00、06:00、12:00、18:00" },
    { "intervalHours": 3, "label": "每天8次", "description": "每3小时整点发送一次" },
    { "intervalHours": 2, "label": "每天12次", "description": "每2小时整点发送一次" },
    { "intervalHours": 1, "label": "每天24次", "description": "每小时整点发送一次" }
  ]
}
```

### 5.2.2 保存自动通知配置
- 方法：`PUT`
- 路径：`/api/tasks/notify/daily-schedule`

#### Body 参数
- `scopeOrgId?: string`
- `enabled: boolean` — 是否启用自动通知（必填）
- `intervalHours: number` — 发送频率，枚举值：`1 / 2 / 3 / 6 / 12`（必填）
- `prefix?: string` — 通知前缀，不填时用默认前缀

#### 返回结构
同查询接口。

### 5.2.3 手动测试自动通知
- 方法：`POST`
- 路径：`/api/tasks/notify/daily-schedule/test`
- 说明：立即执行一次通知发送，用于验证配置是否正确，不影响定时触发记录。

#### Body 参数
- `scopeOrgId?: string`
- `taskDate?: string`
- `prefix?: string`

#### 返回结构
在发送接口结果基础上，附加以下字段：
```json
{
  "testMode": true,
  "schedule": { "...同查询配置返回..." }
}
```

### 5.2.4 自动调度触发接口（内部调用）
- 函数：`runDailyNotifyScheduleTick(now?)`
- 文件：`backend/src/modules/task/notify/notify.routes.ts`
- 说明：由调度器（cron 或外部定时触发）调用，不对外暴露 HTTP 路由。

#### 调度规则
- 仅在每小时前 5 分钟（`getBeijingMinute < 5`）内响应，超过则跳过
- 按每个基地的 `intervalHours` 判断当前北京时间小时是否整除该频率
- 以 `slotKey = "YYYY-MM-DD HH"` 为去重标识，防止同一时段重复发送
- 发送成功后更新 `DailyNotifySchedule.lastTriggeredSlot`

## 5.3 临时任务通知接口

### 5.3.1 预览接口
- 方法：`GET`
- 路径：`/api/tasks/notify/temporary-feishu/preview`
- 文件：`backend/src/modules/task/notify/notify.routes.ts`

#### Query 参数
- `assignmentId: string`
  - 必填
  - 指定一条临时任务 assignment
- `scopeOrgId?: string`
  - 当前实现中用于任务拥有者范围校验

#### 返回结构
```json
{
  "assignmentId": "...",
  "mode": "ACCOUNT",
  "scopeOrgId": "...",
  "prefixPlaceholder": "来自系统提醒",
  "templateTitle": "临时任务标题",
  "total": 18,
  "pendingCount": 10,
  "inProgressCount": 8,
  "boundCount": 15,
  "unboundCount": 3,
  "distinctUserCount": 9,
  "unboundUsers": [
    { "userId": "...", "nickname": "李四", "phone": "139...", "status": "pending", "subjectName": "李四" }
  ]
}
```

### 5.3.2 发送接口
- 方法：`POST`
- 路径：`/api/tasks/notify/temporary-feishu/send`
- 文件：`backend/src/modules/task/notify/notify.routes.ts`

#### Body 参数
- `assignmentId: string`
- `scopeOrgId?: string`
- `prefix: string`

#### 返回结构
```json
{
  "assignmentId": "...",
  "mode": "ACCOUNT",
  "prefix": "来自系统提醒",
  "summary": {
    "total": 18,
    "pendingCount": 10,
    "inProgressCount": 8,
    "boundCount": 15,
    "unboundCount": 3,
    "distinctUserCount": 9,
    "unboundUsers": []
  },
  "results": [
    {
      "feishuConfigId": "...",
      "configName": "河北基地飞书企业",
      "targetCount": 8,
      "successCount": 8,
      "invalidOpenIds": [],
      "messageId": "om_xxx",
      "error": "可选错误信息"
    }
  ]
}
```

---

## 6. 权限与调用限制

## 6.1 通用中间件
所有通知接口统一挂在：
- `authRequired`
- `identityRequired`
- `permissionRequired("task:report:view")`

也就是说：
- 必须已登录
- 必须具备当前身份
- 必须拥有查看任务报表权限

## 6.2 日常任务权限规则
函数：`canManageDailyNotify(roleCode?: string)`

当前允许角色：
- `DEV_ADMIN`
- `HQ_ADMIN`
- `BASE_ADMIN`
- `TEAM_ADMIN`
- `HALL_MANAGER`

此外还会通过 `resolveBaseScopeOrg` 解析基地范围：
- HQ / DEV 可显式选基地
- 其他角色从当前身份组织向上回溯所在基地

## 6.3 临时任务权限规则
当前实现中临时任务通知除了角色校验外，还要求：
- 任务必须是 `TEMPORARY`
- 任务必须是 `active`
- 发送者必须满足任务拥有权校验

任务拥有权规则来自：
- `backend/src/modules/task/assignment/assignment.service.ts`
- 关键函数：`assertTemporaryOwnership`

当前口径：
- 如果 assignment 有 `createdByIdentityId`
  - 当前身份 `identityId` 必须与之相同
- 否则回退到 `createdBy` 与当前 `userId` 匹配

这意味着：
> 当前临时任务通知不是"看板里谁能看谁就能发"，而是"任务发起人 / 发起身份"才能发。

---

## 7. 日常任务通知的人群计算逻辑

## 7.1 核心函数
- `buildDailyNotifyAudience(taskDate, baseOrg)`
- 文件：`backend/src/modules/task/notify/notify.routes.ts`

## 7.2 计算步骤
1. 对日期格式做标准化（将 `/` 替换为 `-`，防止前端传入斜杠格式导致查询失败）
2. 调用 `reconcileDailyAssignments(baseOrg.path)` 对日常任务状态做对账
3. 查询当前基地相关的日常任务 assignment
4. 过滤条件包括：
   - `category = DAILY`
   - `targets.some.orgId = baseOrg.id`
   - `status in (scheduled, active, ended)`
   - `deletedAt = null`
   - `effectiveAt <= 当天补录截止`
   - `endedAt` 为空或晚于当天结束时间
5. 对每个 assignment 调用：
   - `listAssignmentAudienceMembers(prisma, assignment)`
6. 以 `subjectKey` 去重（与看板对齐：同一 subjectKey 仅绑定到第一个 assignment）
7. 查询对应 `TaskRecord`（key 格式：`assignmentId:subjectKey:YYYY-MM-DD`）
8. **跳过**已被免除的记录（`exemption.status = approved / pending`）
9. 实时计算必填子任务完成数（`itemRecords` 中 `status=done && isRequired != false`）
10. **跳过**所有必填子任务已完成的用户（`requiredDoneItems >= totalCount`）
11. 拼出通知行 `NotifyAudienceRow`，按 `userId` 做聚合累加

## 7.3 通知文案中"任务数"的口径说明

这是最容易产生疑问的字段，明确如下：

**通知文案**：`前缀，日常任务共 N 项，您还有 M 项未完成。`

| 字段 | 含义 | 来源 |
|------|------|------|
| **N（总项数）** | 该用户当天所有日常任务中**必填子任务**的总数 | `template.items.filter(item => item.isRequired !== false).length` |
| **M（未完成数）** | 其中尚未完成的必填子任务数 | `N - requiredDoneItems`（已完成的必填子任务数） |

特殊情况：
- 若某日常任务**没有任何必填子任务**（`totalCount = 0`），系统将整条任务视为 1 件，`incompleteCount = 1`，兜底判断使用数据库 `status` 字段（`pending / in_progress`）
- 若用户当天被分配了**多条日常任务**，系统会累加所有任务的 N 和 M（`aggregateMap` 按 `userId` 汇总）

**注意**：这里统计的**只有日常任务（`category=DAILY`）**，不包含临时任务、专项任务或其他类型。

## 7.4 发送分组逻辑

同一飞书配置桶内，日常任务通知**按"N+M"相同的用户分为一批**发送：
- Key：`${totalCount}_${incompleteCount}`
- 相同 N 和 M 的用户合并到一次飞书批量发送调用中
- 不同 N 或 M 的用户各自单独发送，保证每条消息中的数字准确

这意味着：如果基地内有 10 人都是"共 5 项未完成 3 项"，则只需一次飞书 API 调用；但如果这 10 人的 N/M 各不相同，则需要 10 次调用。

## 7.5 关键口径说明
### 为什么日常任务是一人一天只统计一次
因为当前实现里：
- `resolvedSubjectKeys` 的 key 是 `subjectKey`（通常格式为 `USER:{userId}`）
- 同一用户当天重复命中时只保留第一次出现的 assignment 参考（按 `effectiveAt desc` 排序取最新）

### 为什么日常任务不支持"按 assignment 逐条通知"
因为日常通知本质是按"基地 + 当天"来汇总催办，而不是按单条 assignment。

---

## 8. 临时任务通知的人群计算逻辑

## 8.1 核心函数
- `buildTemporarySubjectGroups(...)`
- 文件：`backend/src/modules/task/assignment/temporary-assignment.utils.ts`

临时任务通知必须复用这套现有主体分组逻辑，不能自行重写组织树筛选。

## 8.2 三种模式

### A. `ACCOUNT`
主体类型通常按 `USER` 聚合。

来源包括：
- 明确设置的 `targetUserIds`
- 目标组织范围 `targets.orgPathSnapshot` 下扫描到的账号

### B. `ANCHOR`
主体类型按 `USER` 聚合。

来源包括：
- 目标组织范围下指定角色身份
- 排除组织与排除主播逻辑都会生效

### C. `MANAGER`
主体类型按 `ORG` 聚合。

来源包括：
- `temporarySubjectOrgType` 指定的组织层级
- 每个主体组织再反查其 `visibleIdentities`

## 8.3 当前通知口径
临时任务当前实现规则如下：
- 只允许通知 `assignment.status = active`
- 只通知 `TaskRecord.status in (pending, in_progress)`
- 统计上按主体计算，不做业务层合并
- 预览里显示：
  - 主体总数 `total`
  - 涉及账号数 `distinctUserCount`
  - 未开始数 `pendingCount`
  - 进行中数 `inProgressCount`
  - 未绑定飞书数 `unboundCount`

## 8.4 一个重要实现细节
### 业务统计"不合并"
你们当前定下的产品口径是：
- 一个人命中多条主体，不合并

因此在统计和预览上：
- `total` 统计的是主体数，不是用户数

### 发送层目前仍按 openId 去重批量发
后端当前复用飞书批量发送接口：
- `sendFeishuBatchMessage(config, openIds, text)`

因此同一飞书配置桶内：
- 相同 `openId` 会去重后再发

这意味着：
- 预览与统计口径：按主体
- 实际同一桶的飞书调用：按 `openId` 去重

如果未来要实现：
> 同一用户命中 3 条主体，就真的收到 3 条独立飞书

则需要把临时任务通知改造为：
- 不使用批量接口聚合 openIds
- 改为逐主体逐条发送

这是后续维护中最值得注意的一个点。

---

## 9. 飞书发送实现说明

## 9.1 获取企业 token
函数：`getFeishuTenantAccessToken(config)`

调用飞书接口：
- `POST https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal`

依赖字段：
- `config.appId`
- `config.appSecret`

## 9.2 批量发送消息
函数：`sendFeishuBatchMessage(config, openIds, text)`

调用飞书接口：
- `POST https://open.feishu.cn/open-apis/message/v4/batch_send/`

发送格式：
- `msg_type = text`
- `open_ids = [...]`
- `content.text = 文本`

## 9.3 为什么按飞书配置分桶
因为不同用户可能绑定在不同飞书企业配置下：
- 每个 `feishuConfigId` 对应不同 `appId / appSecret`
- 同一个企业配置共用一个 `tenant_access_token`

因此后端会：
1. 按 `feishuConfigId` 对通知对象分桶
2. 每桶单独换取 token
3. 每桶单独调用一次（或多次）飞书批量发送

---

## 10. 前端服务层说明

文件：`frontend/src/services/task.ts`

## 10.1 日常通知服务
- `notifyApi.getDailyFeishuPreview(taskDate?, scopeOrgId?)`
- `notifyApi.sendDailyFeishu({ taskDate?, scopeOrgId?, prefix })`

## 10.2 自动定时通知服务
- `notifyApi.getDailySchedule(scopeOrgId?)`
- `notifyApi.saveDailySchedule({ scopeOrgId?, enabled, intervalHours, prefix? })`
- `notifyApi.testDailySchedule({ scopeOrgId?, taskDate?, prefix? })`

## 10.3 临时通知服务
- `notifyApi.getTemporaryFeishuPreview(assignmentId, scopeOrgId?)`
- `notifyApi.sendTemporaryFeishu({ assignmentId, scopeOrgId?, prefix })`

## 10.4 前端类型建议重点关注
文件：`frontend/src/types.ts`

与通知直接相关的类型包括：
- `TaskAssignment`
- `TaskRecord`
- `DailyPublishPreview`
- `TemporaryPublishPreview`
- 临时任务看板 / 日常看板相关 response 类型

虽然当前 `DailyFeishuNotifyPreview` 与 `TemporaryFeishuNotifyPreview` 主要定义在 `frontend/src/services/task.ts` 内部，但后续如果通知能力继续扩展，建议把这些类型逐步迁到 `types.ts` 统一管理。

---

## 11. 数据库字段与表说明

以下字段是飞书通知能力依赖的核心数据结构。

## 11.1 `User`
文件：`backend/prisma/schema.prisma`

通知相关字段：
- `id`
- `nickname`
- `phone`
- `status`
- `feishuConfigId`
- `feishuOpenId`
- `feishuUnionId`

用途：
- 判断用户是否激活
- 判断是否绑定飞书
- 按哪个飞书企业发送

## 11.2 `FeishuEnterpriseConfig`
通知相关字段：
- `id`
- `name`
- `appId`
- `appSecret`
- `status`
- `baseOrgId`
- `teamOrgId`

用途：
- 获取企业 token
- 发送飞书消息
- 将用户按企业配置分桶

## 11.3 `TaskAssignment`
通知相关字段：
- `id`
- `category`
- `status`
- `targets`
- `exclusions`
- `templateId`
- `templateVersion`
- `publishedAt`
- `effectiveAt`
- `endedAt`
- `deletedAt`
- `deadlineAt`
- `temporaryMode`
- `targetRoleCodes`
- `targetUserIds`
- `temporarySubjectOrgType`
- `createdBy`
- `createdByIdentityId`
- `createdByOrgId`

用途：
- 判断任务类型与生命周期
- 解析覆盖范围
- 计算人群
- 校验临时任务通知所有权

## 11.4 `TaskAssignmentTarget`
通知相关字段：
- `assignmentId`
- `orgId`
- `orgPathSnapshot`

用途：
- 表示任务投放组织范围
- 用于后端推导组织树覆盖路径

## 11.5 `TaskAssignmentExclusion`
通知相关字段：
- `assignmentId`
- `exclusionType`
- `orgId`
- `orgPathSnapshot`
- `anchorProfileId`

用途：
- 从通知范围中排除组织或主播
- 临时任务 `ANCHOR / MANAGER` 场景尤为关键

## 11.6 `TaskRecord`
通知相关字段：
- `assignmentId`
- `userId`
- `identityId`
- `subjectType`
- `subjectKey`
- `subjectUserId`
- `subjectOrgId`
- `subjectName`
- `subjectOrgType`
- `recordDate`
- `deadlineAt`
- `status`
- `doneItems`
- `totalItems`
- `submittedAt`
- `lastSubmittedAt`

用途：
- 判断当前任务是否已完成
- 区分 `USER / ORG` 主体
- 统计未开始 / 进行中人数

## 11.7 `TaskRecordIdentityLink`
通知相关字段：
- `taskRecordId`
- `identityId`
- `userId`
- `roleCode`
- `orgId`
- `anchorProfileId`

用途：
- 临时任务 `ORG` 主体下找到实际可见接收人
- 支撑管理式任务通知

## 11.8 `DailyNotifySchedule`（新）
文件：`backend/prisma/schema.prisma`

通知相关字段：
- `id`
- `baseOrgId` — 关联基地组织 ID（唯一索引，每个基地一条配置）
- `enabled` — 是否启用自动通知
- `intervalHours` — 发送频率（枚举：`1 / 2 / 3 / 6 / 12`，对应每天 24/12/8/4/2 次）
- `prefix` — 通知前缀文本
- `lastTriggeredSlot` — 最后一次触发的时间槽（格式：`"YYYY-MM-DD HH"`），用于防止同一时槽重复发送
- `createdAt`
- `updatedAt`

用途：
- 存储每个基地的定时通知配置
- 防止重复发送（`lastTriggeredSlot` 去重）
- 支持不同基地独立设置频率和前缀

---

## 12. 关键函数索引

为了方便开发者与 AI 定位，下面给出关键函数索引。

## 12.1 后端
### `backend/src/modules/task/notify/notify.routes.ts`
- `canManageDailyNotify` — 日常任务通知权限判断
- `canManageTemporaryNotify` — 临时任务通知权限判断
- `getFeishuConfigDelegate` — 获取飞书企业配置 Prisma delegate
- `resolveBaseScopeOrg` — 解析当前身份对应的基地组织
- `getFeishuTenantAccessToken` — 获取飞书企业 token
- `sendFeishuBatchMessage` — 调用飞书批量发送接口
- `getDailyNotifyDefaultPrefix` — 生成默认通知前缀
- `getDailyNotifySchedulePayload` — 拼装定时配置返回结构
- `getDailyNotifySlotKey` — 生成时间槽 key（防重复发送）
- `buildDailyNotifyAudience` — 构建日常通知受众列表
- `summarizeNotifyAudience` — 汇总日常通知统计数据
- `executeDailyNotifySend` — 执行一次日常通知发送（手动和自动共用）
- `runDailyNotifyScheduleTick` — 自动定时调度触发函数
- `buildTemporaryNotifyAudience` — 构建临时任务通知受众列表
- `summarizeTemporaryNotifyAudience` — 汇总临时任务通知统计数据
- `resolveTemporaryAssignmentForNotify` — 临时任务权限与状态校验

### `backend/src/modules/task/assignment/temporary-assignment.utils.ts`
- `resolveTemporaryMode`
- `resolveTemporaryRoleCodes`
- `resolveTemporaryTargetUserIds`
- `normalizeTemporaryDraftInput`
- `buildTemporarySubjectGroups`
- `buildTemporaryPreview`

### `backend/src/modules/task/assignment/assignment.service.ts`
- `assertTemporaryOwnership`
- `getTemporaryPublishPreview`
- `publishTemporaryDraft`
- `generateTemporaryRecords`

### `backend/src/modules/task/assignment/daily-assignment.utils.ts`
- `listAssignmentAudienceMembers`
- `isAssignmentOrgExcluded`
- `isAssignmentAnchorExcluded`

### `backend/src/modules/task/record/daily-record-time.utils.ts`
- `formatBeijingDate`
- `getDailyTaskDayEnd`
- `getDailyTaskSupplementDeadline`
- `resolveTaskRecordStatus`

## 12.2 前端
### `frontend/src/pages/tasks/dashboard/DailyTaskDashboardPage.tsx`
- `handleNotifyToday`
- `confirmNotifyToday`

### `frontend/src/pages/tasks/manage/components/DailyTaskWizard.tsx`
- `handleDailyFeishuNotify`
- `confirmDailyFeishuNotify`

### `frontend/src/pages/tasks/dashboard/TemporaryTaskDashboardPage.tsx`
- `handleSendTemporaryNotify`
- `confirmSendTemporaryNotify`
- `ConfirmNotifyModal`

### `frontend/src/pages/tasks/manage/components/TemporaryIssuePanel.tsx`
- `handleSendTemporaryNotify`
- `confirmSendTemporaryNotify`
- 临时任务进行中卡片上的通知按钮渲染逻辑

---

## 13. 当前维护注意事项

## 13.1 不要单独重写临时任务通知的人群算法
临时任务通知一定要复用：
- `buildTemporarySubjectGroups`

否则很容易丢失：
- `ACCOUNT / ANCHOR / MANAGER` 三模式差异
- 组织排除规则
- 主播排除规则
- 可见身份逻辑

## 13.2 日常与临时的通知粒度不同
- 日常任务：基地 + 日期
- 临时任务：单 assignment

不要把两者混成同一套总通知逻辑。

## 13.3 临时任务的"业务统计不合并"和"发送去重"并存
这是当前实现里最容易误解的一点：
- 预览上看，临时任务按主体统计
- 发送上看，飞书批量接口会按 `openId` 去重

后续修改文案、统计口径、消息数量时，必须先确认业务想改的是：
- 统计展示层
- 还是实际飞书发送次数

## 13.4 日常任务通知中"N 项"的来源是必填子任务数
通知文案里的 N 和 M 都来源于模板的**必填 item** 计算，不是所有子任务，也不是其他任务类型：
- `N = template.items.filter(item => item.isRequired !== false).length`
- `M = N - 已完成的必填 item 数`

若某条日常任务无必填子任务，则系统将其整体计为"1 件"处理。

## 13.5 被免除的任务不纳入通知
若用户的 `TaskRecord` 存在 `exemption.status = approved` 或 `pending`，该记录会被直接跳过，不会出现在通知对象列表中。

## 13.6 日常任务发送按 N+M 分组
同一基地、同一飞书企业桶内，日常通知不是所有人共用一条消息，而是按 `totalCount + incompleteCount` 相同的用户合并发送：
- 这样保证每人收到的消息中数字是准确的
- 极端情况下（每人数值不同）可能产生较多飞书 API 调用

## 13.7 自动定时通知的防重复机制
`runDailyNotifyScheduleTick` 使用 `lastTriggeredSlot`（格式 `"YYYY-MM-DD HH"`）作为幂等 key：
- 同一时槽内如果已经发过，`status = "SKIPPED_DUPLICATE_SLOT"`
- 调度器应保证在每小时前 5 分钟内至少触发一次（`getBeijingMinute < 5` 才处理）

## 13.8 弹窗输入前缀是当前统一交互
目前通知前缀的交互已统一为：
- 页面主区域不再长期保留输入框
- 用户点击通知按钮后，在弹窗中填写前缀再确认发送

若后续开发者再次在页面主区域增加输入框，应先确认是否会破坏当前交互一致性。

## 13.9 推荐后续可继续优化的方向
后续如需继续扩展，建议优先考虑：

1. 将日常 / 临时通知弹窗抽成共用组件
2. 将通知预览 / 发送返回类型迁移到 `frontend/src/types.ts`
3. 引入通知发送日志表，记录：
   - 发送时间
   - 发送人
   - assignmentId / taskDate
   - 目标人数
   - 成功 / 失败人数
4. 若产品要求"一人命中多条主体必须收到多条消息"，改造临时任务发送为逐条单发
5. 考虑将自动通知调度迁移为系统内置 cron，而非依赖外部触发

---

## 14. 面向 AI 工具的阅读提示

如果后续让 AI 继续维护飞书通知能力，建议在提示词里明确说明：

1. 当前项目存在两套通知：`daily-feishu` 与 `temporary-feishu`
2. 日常任务还有独立的自动定时通知能力：`daily-schedule`
3. 日常任务与临时任务的人群计算逻辑不同
4. 临时任务通知必须复用 `buildTemporarySubjectGroups`
5. 日常任务通知文案中的"N 项"指**必填子任务数**，不是所有子任务数，也不包括其他任务类型
6. 临时任务当前的产品规则是：
   - 只通知 active assignment
   - 只通知 pending / in_progress
   - 业务统计按主体不合并
   - 文案前缀由发送者决定
7. 日常任务发送前需按 `totalCount+incompleteCount` 分组，保证每人收到的消息数字准确
8. 当前弹窗交互为统一页面内弹层，不应回退为浏览器原生 `window.confirm`

这样 AI 在继续改功能时，更不容易误删规则或重写错误的人群计算路径。

---

## 15. 文档维护建议

建议后续每次修改通知能力时，同步更新本文档以下章节：

- 第 4 章：页面入口与交互位置
- 第 5 章：接口说明
- 第 7 章：日常任务人群计算逻辑与任务数口径说明
- 第 8 章：临时任务人群计算逻辑
- 第 11 章：数据库字段说明
- 第 13 章：维护注意事项

若通知能力后续增加：
- 发送日志
- 模板消息
- 重试机制
- 防重复发送策略
- 未绑定用户列表导出

也建议单独补充新章节，而不要只改代码不改文档。

---

## 16. 流转任务节点完成提醒（2026-09-03）

### 16.1 业务规则

协同任务的流转模式中，每个节点首次完成后，立即向原始任务发布人发送一条飞书文本通知，不必等待整个任务完成。

例如 A 发布“月度资料”，张三负责的节点包含“资料收集”和“附件上传”，该节点完成后，A 收到以下多行文本：

```text
【流转任务·（张三）节点完成】
主任务：月度资料
1问答：资料收集 - 已完成收集
2图片：附件上传 - 请进后台查看
执行人：张三
该节点必填子任务已全部完成。
```

文案取值规则：

- 任务标题：`WorkflowTask.title`
- 执行人：发布时保存的 `WorkflowStep.assigneeName`
- 子任务：仅列出本次完成节点的题型、`WorkflowQuestion.title` 和已保存答案，包含必填和选填，按与任务详情相同的题目顺序编号。提交成功后读取该 stepId 的 `WorkflowAnswer`，按 questionId 匹配，包含最后一次提交的答案；不列出其他节点的子任务。节点名称不作为子任务名称。
- 接收人：`WorkflowTask.createdByUserId`，使用发布人当前绑定的飞书企业配置和 openId

本能力适用于 `WorkflowTask / WorkflowStep`，不包含广播模式、日常或临时 assignment。无需新建配置或开启定时通知；存量流转任务此后新完成的节点同样生效，历史已完成节点不补发。

### 16.2 触发入口与完成口径

文件：`backend/src/modules/task/collaboration/workflow.store.ts`

两个现有入口均已接入：

1. `saveStepQuestionAnswer`：逐题保存，所有必填题均满足现有完成条件时，节点自动完成并提醒；尚未填完仅保存答案。
2. `submitWorkflowStep`：兼容旧版整节点提交接口，在节点完成后提醒。

对应 HTTP 路由仍为：

- `POST /api/tasks/collaboration/workflows/:taskId/steps/:stepId/answer`
- `POST /api/tasks/collaboration/workflows/:taskId/steps/:stepId/submit`

无需新增 HTTP 路由、数据库字段或数据库迁移。

### 16.3 并发、防重与失败行为

完成事务内使用带 `status != completed` 条件的 `workflowStep.updateMany`。只有成功变更状态的请求可以触发通知；重复提交或两个入口并发完成同一节点不会再次触发。

通知在事务成功提交后发送；事务回滚不会发送。飞书异常由通知服务捕获并记录，已经完成的节点不会回滚。发布人未激活、未绑定飞书，或企业配置停用时跳过并记录原因。

当前为即时、尽力发送，无持久化待发队列、自动重试或送达回执。若提交成功后进程在发送前退出，或者飞书发送失败，该条消息不会自动补发；用户重复提交也不会补发。每次 token 请求和消息请求分别有 5 秒超时。

### 16.4 代码与日志

- `backend/src/modules/task/notify/workflow-completion-notify.service.ts`：解析发布人绑定、拼装文案、发送并隔离异常。
- `backend/src/modules/task/notify/feishu-message.service.ts`：有超时限制的飞书 token 获取与批量文本发送；本功能仅传入发布人的一个 openId。
- `backend/src/modules/task/collaboration/workflow.store.ts`：两个节点完成入口、条件更新和事务提交后的通知调用。
- `backend/tests/workflow-completion-notify.test.ts`：隔离数据库和网络的回归测试，运行 `npm run test:workflow-completion --workspace backend`。

运行日志位于 `backend/logs/backend-runtime.log`，事件名为：

- `workflow_completion_notify_sent`：飞书接口接受发送，记录 messageId，不代表已读。
- `workflow_completion_notify_skipped`：发布人不可用、未绑定或配置不可用。
- `workflow_completion_notify_failed`：token、发送请求或 openId 发生错误。

日志包含 taskId、stepId 和 issuerUserId，便于定位具体任务节点。

---

## 17. 临时任务完成提醒（2026-09-03）

### 17.1 业务范围与文案

临时任务的每条 `TaskRecord` 首次变为 `submitted` 后，向原始发布人 `TaskAssignment.createdBy` 发送一条飞书完成提醒。不必等待该 assignment 的所有执行主体完成。提醒独立于催办配置，无需开启自动催办。

模式名复用 `resolveTemporaryMode`：`ACCOUNT` 对应触达式，`ANCHOR` 对应主播式，`MANAGER` 对应管理式；旧任务未存模式时沿用现有模式回退规则。

账号主体示例（主播模式标题改为 `【临时任务·主播式】`）：

```text
【临时任务·触达式】
任务标题：月度资料
1问答：资料收集 - 已完成收集
2学习链接：阅读确认 - 已确认完成
执行人：张三
该任务必填子任务已全部完成。
```

组织主体示例：

```text
【临时任务·管理式】
任务标题：月度资料
1问答：资料收集 - 已完成收集
2学习链接：阅读确认 - 已确认完成
组织：运营一组
提交人：张三
该任务必填子任务已全部完成。
```

字段来源：

- 任务标题与子任务：assignment 对应模板的标题及所有 TaskItem 的题型、标题和已保存答案，按 `sortOrder` 排序，包含选填项。只查询本次完成记录的 itemRecords，按 taskItemId 匹配；附件内容及附件链接不发送。
- USER 主体的执行人：优先 `TaskRecord.subjectName`，回退到记录关联用户昵称或本次提交人昵称。
- ORG 主体的组织：优先 `TaskRecord.subjectName`，回退到 `subjectOrg.name`。
- 提交人：促成这次首次完成的请求所对应用户的昵称；管理式可能由多人协作，故不表述为此人独自完成全部内容。

### 17.2 完成入口与防重

文件：`backend/src/modules/task/record/record.service.ts`。

- `submitItemRecord`：沿用逐项保存口径，已完成子任务数达到 `record.totalItems` 时自动提交，然后触发首次完成提醒。
- `submitRecord`：沿用整条提交口径，检查全部必填项均为 done 后提交，然后触发首次完成提醒。
- `reconfirmRecord`：截止前二次确认不触发完成提醒。

临时任务通过 `saveTemporaryRecordProgress` 条件更新：只有 `status != submitted` 且 `submittedAt = null` 的记录首次成功写为 submitted，才可以发送。并发请求共用这一条件，不按用户或组织名称自行去重。

因此同一组织的多人共同完成一条记录只提醒一次；不同组织对应不同记录，各自完成会分别提醒。同一管理式记录完成后的追加内容和再次提交不再提醒，原始 submittedAt 保留。较早发起的未完成进度更新不会将已完成状态降回进行中。

日常任务继续使用原有记录更新路径，不发送此类完成提醒。无需数据库迁移，不扫描或补发历史已完成记录。

### 17.3 发送与排障

通知服务：`backend/src/modules/task/notify/temporary-completion-notify.service.ts`。

复用 `feishu-message.service.ts`，使用发布人当前的飞书企业配置及 openId。发布人未绑定、账号不可用或企业配置停用时跳过；请求失败时记录错误，不回滚已经完成的任务。即时发送采用与流转提醒相同的 5 秒单请求超时，无自动重试和历史补发。

日志：`backend/logs/backend-runtime.log`，包含 recordId，成功解析记录后还包含 assignmentId、issuerUserId、subjectKey。

- `temporary_completion_notify_sent`：飞书接口接受发送，不代表已读。
- `temporary_completion_notify_skipped`：发布人未绑定或配置不可用等原因。
- `temporary_completion_notify_failed`：读取通知信息、获取 token 或发送消息失败。

测试：`backend/tests/temporary-completion-notify.test.ts`，使用模拟数据库和飞书响应，可运行 `npm run test:temporary-completion --workspace backend`。

### 17.4 流转与临时完成提醒的答案展示规则

两种完成提醒共用 `backend/src/modules/task/notify/completion-answer-format.ts`，每行格式为 `序号题型：题目 - 答案`。只调整展示，不改变完成判定、接收人或防重逻辑。

| 题型 | 提醒中的名称 | 答案展示 |
|------|--------------|----------|
| QA | 问答 | answerText；为空显示“未填写” |
| FILL_BLANK | 待办确认 | 优先显示 answerText；临时记录仅有 done 状态时显示“已确认完成”，否则“未确认” |
| SINGLE_CHOICE | 单选 | 已选选项文字；为空显示“未选择” |
| MULTI_CHOICE | 多选 | 已选选项文字用“、”连接；为空显示“未选择” |
| LINK | 学习链接 | isLinkConfirmed 为 true 时显示“已确认完成”，否则“未确认” |
| ATTACHMENT | 图片 | 统一显示“请进后台查看”，也适用于 PDF、Office 等文件附件，不读取或发送附件内容与链接 |

题目与文字答案的换行及连续空白合并为空格，确保每个子任务占一行。没有填写的选填项保留为空答案提示，不因整条任务完成而自动显示已完成。附件始终保留后台查看提示。
